name: data
version: 1.0.0+1
publish_to: none
description: Data Packages.
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: "2.10.2"

dependencies:
  shared:
    path: ../shared
  text_to_multi_transleting:
    path: ../../

  flutter:
    sdk: flutter
  freezed_annotation: ^1.1.0

dev_dependencies:
  freezed: ^1.1.1
  build_runner: ^2.1.8
  json_serializable: ^6.1.5
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

